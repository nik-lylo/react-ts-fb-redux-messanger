import React from "react";
import { makeStyles } from "@mui/styles";

export const useAddChatStyles = makeStyles({
  add_chat_form_input: {},
  add_chat_card_avatar: {
    width: "43px",
    height: "48px",
    borderRadius: "12px",
  },
  add_chat_card_icon: {
    marginRight: "20px",
    color: "green",
    fontSize: "35px",
    cursor: "pointer",
  },
});
