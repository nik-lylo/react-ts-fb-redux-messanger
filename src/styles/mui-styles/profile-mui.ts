import React from "react";
import { makeStyles } from "@mui/styles";

export const useProfileStyles = makeStyles({
  profile_avatar__avatar: {
    width: "100%",
    height: "100%",
    borderRadius: "0px",
  },
});
