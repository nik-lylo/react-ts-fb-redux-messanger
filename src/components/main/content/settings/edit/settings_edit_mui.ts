import { makeStyles } from "@mui/styles";

export const useSettingsEdit = makeStyles({
  settings_edit__icon_photo: {
    background: "none !important",
    width: "12px !important",
    height: "12px !important",
    color: "white !important",
  },
});
